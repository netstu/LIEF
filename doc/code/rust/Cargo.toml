[package]
name         = "lief-doc-code"
version      = "0.0.0"
edition      = "2024"
rust-version = "1.85.0"
publish      = false

[lib]
path = "src/lib.rs"

[dependencies]
lief = { path = "../../../api/rust/crates/lief" }

[lints.clippy]
field_reassign_with_default = "allow"
